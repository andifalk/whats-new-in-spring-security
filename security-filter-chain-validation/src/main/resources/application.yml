spring:
  application:
    name:
      security-filter-chain-validation
logging:
  level:
    org.springframework.security: trace
management:
  endpoints:
    web:
      exposure:
        include: '*'